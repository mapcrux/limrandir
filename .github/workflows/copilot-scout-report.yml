name: Copilot Daily Campaign Update

on:
  schedule:
    - cron: "30 9 * * *"
  workflow_dispatch:

permissions:
  issues: write
  contents: write

jobs:
  run-copilot-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4

      - name: Install GitHub Copilot CLI
        run: npm install -g @github/copilot

      - name: Execute Copilot Agent
        env:
          # A Personal Access Token (PAT) with 'copilot' and 'repo' scopes
          GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
        run: |
          copilot --agent=scout-report --prompt "Perform the daily tasks and update the local files" --model=claude-sonnet-4.6 --yolo
          
      - name: Commit and push updated files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A

          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "chore: generate daily scout report"
          git push
